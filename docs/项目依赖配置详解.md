这是一个典型的 **Expo React Native 项目**的 `package.json` 文件，使用了最新的 **Expo Router**（基于文件系统的路由）。让我详细解释各个部分：

## 🏷️ **基本信息**

```json
"name": "expo-starter",
"version": "1.0.0",
"private": true
```

- 项目名称为 `expo-starter`
- 版本号 1.0.0
- `private: true` 防止意外发布到 npm

## 📦 **核心依赖 (dependencies)**

### **React 生态**

- `react@19.1.0` & `react-dom@19.1.0` - React 19 版本
- `react-native@0.81.5` - React Native 框架

### **Expo 核心**

- `expo@~54.0.30` - Expo SDK 54，用于构建跨平台应用
- `expo-router@~6.0.21` - **Expo 文件式路由系统**（核心特性）
  - `"main": "expo-router/entry"` 表示使用 Expo Router 作为入口

### **导航系统**

- `@react-navigation/*` - React Navigation 套件
  - 用于标签页导航、原生导航等
  - 与 `expo-router` 配合使用

### **性能与动画**

- `react-native-reanimated` - 高性能动画库
- `react-native-gesture-handler` - 手势处理
- `react-native-screens` - 原生屏幕组件，提升性能

### **UI 与样式**

- `expo-image` - 高性能图片组件
- `@expo/vector-icons` - 图标库
- `expo-symbols` - SF Symbols（苹果系统图标）
- `expo-system-ui` - 系统 UI 管理

### **功能模块**

- `expo-font` - 自定义字体加载
- `expo-haptics` - 触觉反馈（振动）
- `expo-linking` - 深层链接处理
- `expo-web-browser` - 内置浏览器打开链接
- `expo-splash-screen` - 启动屏幕控制
- `expo-status-bar` - 状态栏管理
- `expo-constants` - 应用常量访问

### **布局与安全区**

- `react-native-safe-area-context` - 适配刘海屏、安全区域

## 🛠️ **开发依赖 (devDependencies)**

```json
"devDependencies": {
  "@types/react": "~19.1.0",
  "typescript": "~5.9.2",
  "eslint": "^9.25.0",
  "eslint-config-expo": "~10.0.0"
}
```

- **TypeScript** 支持
- **ESLint** 代码检查，使用 Expo 的配置规则

## 📜 **脚本命令 (scripts)**

```json
"scripts": {
  "start": "expo start",           // 启动开发服务器
  "android": "expo start --android", // 启动 Android
  "ios": "expo start --ios",       // 启动 iOS 模拟器
  "web": "expo start --web",       // 启动 Web 版本
  "reset-project": "node ./scripts/reset-project.js", // 重置项目
  "lint": "expo lint"              // 代码检查
}
```

## 🚀 **项目特点**

### **1. 跨平台支持**

- ✅ iOS、Android、Web 三端支持
- 使用 `expo start --ios/android/web` 分别启动

### **2. 现代化路由**

- 使用 **Expo Router**（类似 Next.js 的文件式路由）
- 项目结构基于 `app/` 目录自动生成路由

### **3. 完整的开发体验**

- 内置 TypeScript 支持
- ESLint 代码规范
- 热重载、开发工具

### **4. 丰富的功能模块**

- 包含图片、字体、图标、动画等常用功能
- 导航、手势、安全区等移动端必备组件

## 💡 **使用建议**

1. **启动项目**：

   ```bash
   npm start  # 或 yarn start
   ```

2. **项目结构**：

   - 路由文件应放在 `app/` 目录下
   - 使用 `app/index.js` 作为首页

3. **平台特定开发**：
   ```bash
   npm run ios     # 开发 iOS
   npm run android # 开发 Android
   npm run web     # 开发 Web 版本
   ```

这是一个**现代化、功能齐全的 Expo 起始模板**，特别适合需要快速开发跨平台应用，且希望使用文件式路由的开发者。
